var tools = {
  valueText: function (item) {
    let icon = "-"
    if (item.type === 1) {
      icon = "-"
    } else if (item.type === 2) {
      icon = "+"
    }

    return icon + item.value
  }
}

module.exports = {
  valueText: tools.valueText
}

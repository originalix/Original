var foo = "'hello world' from comm.wxs";
var bar = function(d) {
  return d;
}
module.exports = {
  foo: foo,
  bar: bar
};

var foo = 1;
var bar = "hello world";
var i; // i === undefined

var a = 10, b = 20;
